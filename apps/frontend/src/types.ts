type PostType = {
  post_id: string;
  thumbnail_url: string;
  title: string;
  description: string;
  github_link: string;
  live_demo_link: string;
  tags: string[];
  created_at: string;
};

export type { PostType };
